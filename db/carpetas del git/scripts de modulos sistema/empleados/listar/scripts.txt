Modulo: empleados
Operacion: listar
Generado: 2026-03-23 01:06:47

Sentencias SQL vigentes:

SQL [1] - app/controllers/empleadoController.php -> listarEmpleadoControlador
SELECT
                e.id_ai_empleado,
                e.id_empleado,
                e.nacionalidad,
                e.nombre_empleado,
                e.telefono,
                e.correo,
                e.direccion,
                COALESCE(c.nombre_categoria, 'SIN CATEGORIA') AS nombre_categoria,
                CASE
                    WHEN u.id_ai_user IS NULL THEN 'NO ASOCIADO'
                    ELSE CONCAT('@', u.username)
                END AS usuario_sistema
             FROM empleado e
             LEFT JOIN categoria_empleado c
               ON c.id_ai_categoria_empleado = e.id_ai_categoria_empleado
             LEFT JOIN user_system u
               ON u.id_empleado = e.id_empleado
              AND u.std_reg = 1
             WHERE e.std_reg = 1
             ORDER BY e.nombre_empleado ASC
