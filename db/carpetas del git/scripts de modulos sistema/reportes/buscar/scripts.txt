Modulo: reportes
Operacion: buscar
Generado: 2026-03-23 01:06:47

Sentencias SQL vigentes:

SQL [1] - app/controllers/cargarFiltrosReporte.php
SELECT COUNT(1) FROM INFORMATION_SCHEMA.COLUMNS
            WHERE TABLE_SCHEMA = DATABASE()
              AND TABLE_NAME = :t
              AND COLUMN_NAME = :c

SQL [2] - app/controllers/cargarFiltrosReporte.php
SELECT
            id_empleado AS id_user,
            COALESCE(NULLIF(nombre_empleado, ''), id_empleado) AS user,
            username
         FROM vw_usuario_empleado
         WHERE std_reg = 1
         ORDER BY COALESCE(NULLIF(nombre_empleado, ''), id_empleado) ASC

SQL [3] - app/controllers/cargarDatosReporte.php
SELECT COUNT(1) FROM INFORMATION_SCHEMA.COLUMNS
            WHERE TABLE_SCHEMA = DATABASE()
              AND TABLE_NAME = :t
              AND COLUMN_NAME = :c

SQL [4] - app/controllers/cargarDatosReporte.php
SELECT {{ $mm->columnasTablaSql('empresa_config') }} FROM empresa_config WHERE id = 1 LIMIT 1

SQL [5] - app/controllers/cargarDatosReporte.php
SELECT *
              FROM vw_ot_resumen
              WHERE n_ot = :id AND std_reg = 1
              LIMIT 1

SQL [6] - app/controllers/cargarDatosReporte.php
SELECT
                    id_ai_detalle,
                    fecha_detalle AS fecha,
                    nombre_turno,
                    COALESCE(NULLIF(usuario_act_nombre, ''), id_user_act) AS tecnico_nombre,
                    miembro_cco_nombre AS cco_nombre,
                    miembro_ccf_nombre AS ccf_nombre,
                    descripcion,
                    hora_inicio,
                    hora_fin,
                    observacion
                  FROM vw_ot_detallada
                  WHERE n_ot = :id
                  ORDER BY id_ai_detalle ASC

SQL [7] - app/controllers/cargarDatosReporte.php
SELECT {{ $mm->columnasTablaSql('miembro') }}{{ " FROM miembro {$where} ORDER BY nombre_miembro ASC LIMIT 800" }}
