Modulo: ordenes_trabajo
Operacion: eliminar

Sentencias SQL:

-Ordenes de trabajo:
UPDATE orden_trabajo SET std_reg = 0 WHERE n_ot = :id;

-Detalle de Ordenes de trabajo:
DELETE FROM detalle_orden WHERE n_ot = :not AND id_ai_detalle = :id AND fecha = :fecha;
